# home.local
# 🚀 Home Server — System &amp; Network Design  A self-hosted home server running on a mini PC (Ubuntu Linux) with Docker Compose. The system provides media streaming, file storage, photo management, automation, DNS ad-blocking, and remote access — all behind a local HTTPS reverse proxy.
